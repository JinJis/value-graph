/**
 * @valuegraph/ui — shared design tokens and components, including the
 * confidence/freshness legend that the Terminal canvas relies on.
 *
 * [M0-REPO-01] scaffold placeholder; real tokens/components arrive alongside the
 * Terminal milestones (M5+).
 */

/** Marker export so the package has a typed surface for scaffolding checks. */
export const UI_PACKAGE_VERSION = "0.0.0" as const;
