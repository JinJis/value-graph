// Stub boot for ValueGraph Terminal (User). The real Next.js + React Three Fiber
// 3D canvas arrives in [M0-API-05]; this only proves the workspace is wired ([M0-REPO-01]).
console.log(
  "ValueGraph Terminal — stub boot OK ([M0-REPO-01]); Next.js + R3F app lands in [M0-API-05].",
);
